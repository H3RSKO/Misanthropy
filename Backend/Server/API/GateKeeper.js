// route middleware to check if there is a cookie

// function hasCookie(req, res, next) {
//   if (req.sessionID) return next()
//   res.redirect('/')
// }

// module.exports = {
//   hasCookie
// }
